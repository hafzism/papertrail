# W1 foundation test results

Recorded: 2026-09-20

| Command | Result | Evidence |
|---|---|---|
| `pnpm test` | pass | 47 unit tests: predicate three-valued logic, metered Responses failure paths, activity consent/trigger guards, durable worker behavior, packet manifests, AcroForm inspection, and extraction text/page-metadata guards |
| `pnpm check` | pass | TypeScript checks for contracts, domain, AI, database, and web packages |
| `pnpm --filter @papertrail/web build` | pass | Next.js production build: accessible status/login/protected-route shell, Supabase SSR proxy, PKCE callback, private vault, and authenticated application draft routes compile |
| `pnpm test:integration` | pass | Disposable Docker Postgres migration/RLS test: partial A01–A04 including private storage, server-owned document intake/outbox, direct lifecycle-transition denial, direct private-source writes, unresolved-text capture, public-URL candidate ownership, and loopback URL rejection; candidate-activity boundary, A16/A18/A42 duplicate-claim, stale-fence, expiry-recovery, and supported-kind filtering, A35 concurrent reservation, and A36 uncertain-charge behavior |
| `npx supabase@latest db push --linked --yes` | pass | Applied `202609200014_public_source_capture_jobs.sql` to the linked managed project. This is deployment evidence only; guarded public-source capture remains disabled until its own live smoke. |
| `pnpm --filter @papertrail/db check`; `pnpm --filter @papertrail/worker check`; worker test; `pnpm test:integration` | pass | Follow-up validation after worker heartbeat capability binding fix. The remaining live evidence is a restarted worker against the configured Session Pooler. |
| `pnpm --filter @papertrail/worker check` and worker test | pass | Error observability and JSON-parameter follow-up: worker preserves a bounded handler error code and binds extraction metadata as JSON documents; 6 worker tests pass. |
| Owner-operated private PDF smoke | pass | The worker logged `worker.job_finished` with `state: "succeeded"`; the owner vault displayed `completed`, three detected pages, and the bounded extracted-text preview. This establishes the actual upload → queue → worker → private artifact/result path for a text PDF. |
| Web/worker checks, production build, and database integration suite | pass | The worker now has eight focused tests, including image MIME/signature validation before OCR. |
| Local OCR diagnostic | pass | With Tesseract English/Malayalam data installed, a read-only private scanned-PDF diagnostic completed one page and produced 1,633 characters. The document's existing terminal job state was not altered. |
| Local direct-image OCR smoke | pass | A generated local PNG containing `PaperTrail image OCR 2026` was read correctly by the installed English Tesseract engine. No file was uploaded and the temporary files were removed. |
| Private-document removal isolation | pass | Local database integration confirms direct DML is denied; owner removal hides the document/version and returns only its owner-scoped cleanup paths. Migration `202609200019_private_document_removal.sql` was then applied to the linked Supabase project. |
| Requirement citation provenance | pass | Local integration verifies new unresolved requirements retain a valid source hash and explicitly report a `registered_url` basis where only a URL candidate exists. Migration `202609200020_private_requirement_citation_provenance.sql` was applied to the linked project. |
| Restricted requirement predicates | pass | Local integration verifies owner-created requirement records default to `manual_review`; database validation rejects malformed/unknown predicate structures before they can later be evaluated. Migration `202609200021_private_requirement_predicates.sql` was applied to the linked project. |
| Captured-source final URL provenance | pass | Nine worker tests verify the final, safe redirect destination is carried into the fenced capture write. Migration `202609200022_public_source_final_url.sql` persists it with the captured content hash. |
| Owner-confirmed profile facts | pass | Local integration verifies direct fact DML is denied and the owner-only function records a private, append-only confirmed owner assertion. Migration `202609200023_owner_profile_facts.sql` was applied to the linked project. |
| Application-specific fact overrides | pass | Type checks, unit tests, the disposable database isolation suite, and the production web build pass. Direct override DML is denied; the owner-only RPC records a private append-only override for a draft application and creates its application event. Migration `202609200024_application_fact_overrides.sql` was applied to the linked project. |
| OpenAI configuration/token count | pass | The configured project key successfully completed the official Responses input-token count request for the selected model. No generated output was requested. |
| First metered OpenAI generation | charge uncertain | The durable reservation is retained at a maximum of $0.00003905 and no automatic retry was made. A local parser correction now accepts raw `output[].content[].text` Responses output and has focused unit coverage; a new provider call requires explicit owner retry authorization. Migration `202609200025_default_budget_campaign.sql` created the fixed $5 campaign cap and was applied to the linked project. |
| Owner-authorized metered OpenAI retry | pass | The corrected transport completed the fixed acknowledgement smoke and settled measured usage at $0.00002380. The earlier uncertain reservation remains preserved for reconciliation; it was not overwritten or retried automatically. |
| Requirement-proposal contract | pass | Versioned prompt contract plus deterministic JSON/field/citation validation. Eleven domain tests pass, including rejection of a citation that is not a literal substring of the private source. |
| Private requirement proposal workflow | pass | Five AI, eleven domain, and twelve worker tests plus the disposable database suite and production web build pass. The database test verifies direct proposal DML denial, owner-scoped queue/outbox creation, fenced worker recording, exact `owner_text` citation provenance/manual-review defaults, and explicit owner acceptance. Migration `202609200026_private_requirement_proposals.sql` was applied to the linked project. |
| Packet manifest, RuleDiff, activity, and settings follow-up | pass | The full workspace gate passed: five AI, twenty-two domain, and fifteen worker unit tests; recursive TypeScript check; disposable database integration; production web build; and whitespace validation. The integration suite verifies direct packet-artifact DML denial, owner-scoped request/worker fence/record flow, owner isolation, and stale/cleanup handling when a bound document is removed. Migrations `202609200027_private_packet_manifest.sql` and `202609200029_owner_tracked_activity_intake.sql` were applied to the linked project. The restarted worker advertises the tested `prepare_packet_manifest` capability alongside existing enabled kinds. |
| Packet-manifest dependency DAG | pass | Disposable database integration verifies each persisted packet manifest has a private reverse requirement-to-artifact edge. Migration `202609200030_dependency_edges_packet_manifest.sql` adds owner/application scoping, reverse indexes, and database cycle rejection; it is applied to the linked project. |
| Targeted private dependency impact | pass | Disposable database integration applies one requirement-triggered impact and verifies that it returns/stales only the dependent packet artifact. Migration `202609200031_private_dependency_impacts.sql` records owner-readable impacts, protects direct DML, and is applied to the linked project. |
| Interactive AcroForm engine | pass | Real generated interactive PDFs verify field inspection and non-flattened filling of text, checkbox, and dropdown controls. Signed/XFA marker and non-Latin input paths are explicitly routed to assisted handling. The full workspace gate now has five AI, twenty-four domain, and fifteen worker unit tests plus a production web build. |
| Owner-requested private form inspection | pass | The complete workspace gate passed: five AI, twenty-four domain, and eighteen worker unit tests; disposable database integration; production web build; and whitespace validation. The integration suite verifies direct inspection DML denial, owner-scoped request/worker-fence/result flow, and cross-owner isolation. Migration `202609200032_private_form_inspection.sql` was applied to the linked project, and the restarted worker advertises the tested `inspect_acroform` capability. |
| Owner-confirmed form-field mappings | pass | The complete workspace gate passed after form-option inspection support and mapping persistence: five AI, twenty-four domain, and eighteen worker unit tests; disposable database integration; production web build; and whitespace validation. The integration suite verifies direct mapping DML denial, fact-value type checks, owner-scoped snapshots, and cross-owner isolation. Migration `202609200033_owner_form_field_mappings.sql` was applied to the linked project. |
| Owner view of service-created packet artifacts | pass | The disposable database integration verifies that an owner may read only the matching `private-artifacts` storage object through its immutable owner-scoped artifact record, while a second owner cannot read either the record or bytes. Migration `202609200034_private_artifact_owner_read.sql` was applied to the linked project. |
| Owner-operated private packet-manifest smoke | pass | The authenticated owner generated and loaded the existing manifest from the `private-artifacts` bucket. The rendered JSON shows the correct application identity, an empty evidence inventory for this draft, and `submissionReady: false`; it is therefore demonstrably review-only rather than a submission claim. |
| Fenced private AcroForm derivative preparation | pass | Type checks, 51 unit tests, production web build, and the disposable database suite pass. The integration suite verifies owner-only request/fence/record flow, immutable mapped-fact input, review-only artifact status, passed render status, direct-DML denial, and cross-owner isolation. Migrations `202609200035_private_acroform_derivatives.sql` and `202609200036_fix_acroform_derivative_begin_state.sql` were applied to the linked project. |
| Private derivative source-removal impact | pass | Disposable database integration verifies that removing the source document marks its review-only AcroForm derivative stale and returns only that private artifact path for owner-controlled storage cleanup. Migration `202609200037_stale_private_acroform_derivatives.sql` was applied to the linked project. |
| `npx supabase@latest db push --linked --yes` | pass | Applied migrations `202609200015_document_extraction_owner_retry.sql` through `202609200018_evidence_binding_owner_confirmation.sql`. |

This is local foundation evidence only. It is not a managed Supabase deployment result, OpenAI account smoke result, browser flow test, or W1 sign-off.
